rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leer y escribir libremente (para pruebas/MVP)
    // En producción se recomienda restringir por auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
